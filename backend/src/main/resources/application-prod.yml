spring:
  datasource:
    # 生产环境：使用环境变量配置数据库连接
    url: jdbc:mysql://${DB_HOST:47.107.131.134}:${DB_PORT:3306}/${DB_NAME:southside_cart}?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:jfq123}
    hikari:
      minimum-idle: 10
      maximum-pool-size: 50
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
  
  jpa:
    hibernate:
      ddl-auto: validate  # 生产环境使用 validate，不自动修改表结构
    show-sql: false       # 生产环境不显示 SQL
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    root: WARN
    com.shoparro: INFO
    org.springframework.web: WARN
    org.hibernate: WARN

